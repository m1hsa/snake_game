[package]
name = "snake_game_tui"
version = "0.1.0"
edition = "2024"
description = "snake game in terminal"
license = "GPL-3.0-only"

[dependencies]

[profile.release]
lto = true
panic = "abort"
opt-level = "z"
codegen-units = 1
strip = "symbols"
